#include "point.h"

/*int Point::aGauche(const Point &a, const Point &b) const {
    int determinant = (b.x() - a.x())*(this->y() - a.y()) - (this->x() - a.x())*(b.y() - a.y());
    // Pour avoir les résultats à gauche il faut changer le signe du déterminant
    determinant *= -1;
    if (determinant > 0)
        return 1;
    else if (determinant < 0)
        return -1;
    else
        return 0;
}*/
